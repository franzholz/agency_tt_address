
  # cat=basic/enable; type=string; label=Image upload folder: This is the place where the images are stored.
imageFolder = uploads/tx_agencyttaddress
  # cat=basic/enable; type=boolean; label=Use image folder: Enable the usage of imageFolder for the upload of images.
useImageFolder = 0
  # cat=basic/enable; type=string; label=Address table: Name of the address table to be used in the backend.
addressTable = tt_address
  # cat=directMail; type=boolean; label=Support Direct Mail: You must set this only when Direct Mail is not installed at the moment, and if you still want to use the Direct Mail tables in the BE. They must have been generated by a former installation of Direct Mail. Do not enable it if the tables needed by Direct Mail are missing! Install DirectMail at first, add the tables in the Install Tool and deinstall it immediately afterwards again.
enableDirectMail = 0
